{
  "name": "base",
  "version": "1.0.0",
  "scripts": {
    "cleanup": "rmdir /q /s .parcel-cache dist || echo 'no cached files'",
    "start": "npm run cleanup && parcel src/index.html --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.13.2",
    "parcel": "^2.13.2",
    "sharp": "^0.33.5"
  }
}
